# ProleCoder VS Code Extension

This package contains the VS Code frontend for ProleCoder.

The extension starts and supervises the Rust JSON-RPC agent server, renders agent events in the sidebar chat, and forwards user decisions such as approvals back to the shared core.

For repository setup, development plans, and release status, see the workspace root README.
